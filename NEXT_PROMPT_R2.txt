Members 页面打不开，点进去只剩「Members / Manage your organization members and their roles.」一行字，下面整片空白，连侧边栏 3 个链接也消失了。Billing 页面能渲染，但里面所有数据（free / team plan 价格、0/1000 配额、空发票列表）都是前端默认填的，不是后端真实返回的。Dashboard / Audit Log / Settings 几个页面都正常。检查修复一下。
